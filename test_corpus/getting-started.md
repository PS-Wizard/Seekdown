# Installation

Install seekdown with cargo.

```bash
cargo install seekdown
```

This installation guide explains how to install the project with cargo and verify the install step.

# Query Latency

Query latency depends on corpus size, segmentation mode, and ranking model.
Median latency is the main performance metric in the benchmark.
